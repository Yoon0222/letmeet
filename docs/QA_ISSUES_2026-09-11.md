# QA 이슈 로그 — 2026-09-11 (전 기능 스윕)

> 규칙: **발견된 이슈는 기록만, 즉시 수정하지 않음** (사용자 지시). 심각도 High(반드시)/Med(권장)/Low(선택).
> 대상: dev 빌드(troy 계정, Fold6 미러링) + qa-pickle 정적/코드 QA. 코트·대회 전부 무료(fee 0) 전환 상태.
> 초점: 버그/런타임 에러, **화면가림(레이아웃 잘림·겹침·클리핑)**, 다양한 기능 동작.

## 요약
- 정적: 모바일·web-admin tsc/lint **전부 통과**. 런타임 크래시·레드스크린 **없음**.
- 기기 UI 스윕: 주요 화면 9종 **레이아웃 이상 없음**(앱 자체엔 화면가림 버그 미발견).
- 서버/DB 다계정 로직: 정원·대기열·승격·알림·자동종료 **전부 PASS**.
- 발견 이슈: **High 1**(사업자 주소 3곳 불일치), **Med 1**(지원 CC 개인 gmail), **Low 4**.

## 이슈 목록

### 🔴 High
| # | 위치 | 이슈 |
|---|------|------|
| H1 | ~~`src/components/business-footer.tsx:10`~~ · ~~web-admin 랜딩~~ · 기준=`refund-policy` | **[✅ 수정됨 2026-09-11]** 사업자 주소 3곳 불일치(앱="목지3로", 랜딩="경인대로3…물류동", 환불정책="묵지3로…불로동)")를 **환불정책 기준 `인천광역시 검단구 묵지3로 3, 1동 4층 401호(불로동)`으로 통일**(사용자 지시). ⚠️ 여전히 **사업자등록증 실제 표기와 일치하는지 최종 확인 권장**(PG/심사 요건). ⚠️ 랜딩·환불정책은 `NEXT_PUBLIC_BUSINESS_ADDRESS` env가 세팅돼 있으면 그 값이 우선 — Vercel env 값도 동일한지 확인 필요. |

### 🟡 Med
| # | 위치 | 이슈 |
|---|------|------|
| M1 | `src/app/support.tsx:13,17` | 지원 mailto의 `cc`에 운영자 **개인 gmail**(troy.yoonsik.shin@gmail.com)이 남아, 문의 시 사용자 메일 작성창 CC에 노출. 11행 주석상 "수신 안전망"으로 의도됐으나 이메일 통일(pinut@pinut.org) 취지와 상충. pinut 수신함 확보 시 제거 검토. |

### 🟢 Low
| # | 위치 | 이슈 |
|---|------|------|
| L1 | `src/contexts/auth.tsx:57-60` | `loadProfile`에서 profiles 조회 실패 시 `contactReady=true` 전에 early return → `needsPhone` 계속 false → **전화번호 게이트 우회**(다음 성공 로드까지). 게이트가 보안 아닌 온보딩이라 영향 제한적. 제안: 전화번호 로드/`setContactReady`를 profiles 실패와 분리(finally). |
| L2 | `src/app/_layout.tsx:69` + `auth.tsx` | 로그인 직후 `contactReady=false` 구간 동안 미입력 사용자도 (tabs)가 잠깐 보였다 온보딩으로 튕김. 의도된 트레이드오프(허위 온보딩 방지). 순간 깜빡임, 기능 버그 아님. |
| L3 | `web-admin/lib/knockout.ts` | 자동종료 방어적 미발동 엣지: 결승이 부전승만으로 결정되는 degenerate 브래킷(생성 경로에서 미호출), 또는 한 대회 다중 디비전 결승(finals.length>1). 비현실적·수동종료 가능. |
| L4 | `src/app/profile/edit.tsx:129-132` | 프로필 저장 시 유효 휴대폰 필수 → phone 비면 다른 필드 수정도 저장 불가. 온보딩 게이트가 존재 보장하므로 정상 흐름엔 무해. |

### UI 스윕 결과 (troy, 이상 없음)
- 홈: 일정·코트(전부 무료 반영)·대회 카드 정상 / 코트 상세(김포): 무료 표시·빈 슬롯 안내 정상
- 대회 상세: 정보 탭 **내 현황 카드**(참가확정·조·전적·조순위, 스탯타일 3개)·참가비 무료 반영·🔒대진확정 취소잠금 안내 / 참가자 12명 목록 / 순위(조별 순위표·경기결과·코트뱃지·내경기 하이라이트) — 전부 정상
- 모임(번개): 일반/DUPR 탭·빈상태·FAB 정상 / 커뮤니티: 카테고리 칩·게시글 카드 정상 / 전체: 프로필·3타일·설정메뉴 정상
- 프로필 수정(전화번호 필드)·온보딩(전화번호 게이트)·결제창(테스트환경) — 앞서 실검증 완료
- ※ 미러링에 뜨는 우측 톱니(expo dev-client 플로팅 메뉴)는 콘텐츠와 겹치나 **dev 전용 오버레이**, 운영 빌드엔 없음(앱 버그 아님)

### 서버/DB 로직 (다계정 트리거) — 전부 PASS
- 정원 제한·대기열 분리: 무료 정원3에 5신청 → 3확정/2대기 (enforce_waitlist)
- 대기열 자동 승격: 확정1 취소 → 대기1순위(테스터04) 승격 + "참가 확정 🎉" 알림 생성 (promote_waitlist·push_on_notification)
- 본선 결승 확정 시 자동 종료 (maybeFinishTournament, 별도 검증)
- 유료 결제: 신청→결제대기(pending+24h+슬롯점유)·결제창 로드. 실카드 승인은 SIM 없어 미완, 확정은 서버 시뮬레이션으로 UI 검증

## 검증 못한 것 (제약)
- **다계정 UI 로그인**: 비번 입력 대행 금지 → 타 계정 화면은 DB/시뮬레이션으로 대체
- **실카드 결제 승인·환불**: SIM/카드 없음
- **코트 예약 완주**: 운영자가 예약 슬롯(날짜)을 안 열어둠 → 슬롯 개설 후 재검증 필요(버그 아님)
